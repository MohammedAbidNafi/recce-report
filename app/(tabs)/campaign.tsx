import { SafeAreaView, Text } from "react-native";

export default function CampaignScreen() {
  return (
    <SafeAreaView>
      <Text className="text-black">Campaign Screen</Text>
    </SafeAreaView>
  );
}
